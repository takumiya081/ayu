query TestSearchRiverQuery($query: String!) {
  searchRiver(query: $query) {
    id
    location {
      lat
      lng
    }
    name
    union {
      id
      name
      rivers {
        id
      }
    }
  }
}
